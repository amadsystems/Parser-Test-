# Parser-Test-
This project implements a basic arithmetic expression parser and evaluator in Python. It reads a user-input mathematical expression, converts it into tokens using a lexer, and evaluates the result using a recursive descent parser.  The program supports standard arithmetic operations and respects operator precedence and parentheses.
